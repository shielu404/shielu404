import platform
import os
os. system('termux-setup-storage')


arc=str(platform.uname().machine)
if'arm in arc:
      _import_("Dumpp32")._login()
elif 'aarch' in arc:
         _import_(Dumpp"32).login()
else:
       exit(f' Unknown device machine {arc}')
